# Spend Sentinel (Free) — environment configuration
# Copy to `.env` and adjust. All values are optional with sensible defaults.

# Where the local JSON data store lives. Default: ./data
SENTINEL_DATA_DIR=./data

# Port for the local web dashboard (`npm run web`). Default: 4317
SENTINEL_PORT=4317
